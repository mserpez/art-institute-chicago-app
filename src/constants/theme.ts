const SIZE_VALUE = 8;

const theme = {
  colors: {
    main: '#2196f3',
    textPrimary: '#444444',
    textSecondary: '#777777',
  },
  unit: (value: number) => value * SIZE_VALUE,
  radius: 3,
};

export default theme;
